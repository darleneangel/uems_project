name: uems_project
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  supabase_flutter: ^2.0.0
  path: ^1.8.3
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  postgres: ^3.0.0

  # Email Stack
  mailer: ^6.0.1
  
  # PRODUCTION CAMERA STACK
  camera: ^0.11.0+2
  camera_windows: ^0.2.1+9 
  zxing_lib: ^1.1.4
  image: ^4.1.3
  mobile_scanner: ^5.0.0
  
  # UI & Utilities
  cupertino_icons: ^1.0.8
  google_fonts: ^8.0.0
  lucide_icons: ^0.257.0
  image_picker: ^1.1.2
  file_picker: ^8.1.7
  pdf: ^3.10.1
  path_provider: ^2.1.2
  open_file: ^3.3.0
  qr_flutter: ^4.1.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo (2).png